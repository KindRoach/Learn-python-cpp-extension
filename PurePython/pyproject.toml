[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "my_package"
version = "0.0.1"
description = "My pure python package."
dependencies = [
    "numpy"
]

[project.optional-dependencies]
dev = [
    "pytest"
]